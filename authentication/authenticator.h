#pragma once
#include <iostream>
#include <string>

namespace authenticator {
    bool labelHandler(std::string);
    bool aInstruction(std::string);
    bool cInstruction(std::string);
    bool instruction(std::string);
}